const testcase = {
    description: "Search user for API Test",
    positive: {
        case1 : "Search user with valid id"
    },
    negative: {
        case1: "Search user with invalid id",
    }
};

module.exports = {
    testcase
}